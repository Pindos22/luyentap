//
// Created by huydo on 4/15/2024.
//
#include "stdio.h"

/*int main() {

    int a, b, c, max;

    printf("Nhap so can so sanh: ");
    scanf("%d", &a);

    printf("Nhap so can so sanh: ");
    scanf("%d", &b);

    printf("Nhap so can so sanh: ");
    scanf("%d", &c);

    if (a >= b && a >= c)
        max = a;
    else if (b >= a && b >= c)
        max = b;
    else
        max = c;

    printf("So lon nhat la: %d", max);

    getchar();
}*/